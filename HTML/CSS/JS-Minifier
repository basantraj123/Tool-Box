<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HTML/CSS/JS Minifier</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f0f8ff;
      margin: 0;
      padding: 20px;
      color: #003366;
    }

    .container {
      max-width: 800px;
      margin: auto;
      background: white;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 4px 10px rgba(0, 102, 204, 0.2);
    }

    h2 {
      text-align: center;
      color: #007bff;
    }

    textarea {
      width: 100%;
      height: 200px;
      padding: 10px;
      font-size: 14px;
      border-radius: 8px;
      border: 1px solid #ccc;
      resize: vertical;
      margin-bottom: 10px;
    }

    button {
      width: 100%;
      background-color: #007bff;
      color: white;
      border: none;
      padding: 12px;
      font-size: 16px;
      border-radius: 8px;
      cursor: pointer;
    }

    button:hover {
      background-color: #0056b3;
    }

    @media (max-width: 600px) {
      textarea {
        font-size: 13px;
      }
      button {
        font-size: 15px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>HTML / CSS / JS Minifier</h2>
    <textarea id="inputCode" placeholder="Paste your HTML, CSS, or JS code here..."></textarea>
    <button onclick="minifyCode()">Minify</button>
    <textarea id="outputCode" placeholder="Minified code will appear here..." readonly></textarea>
  </div>

  <script>
    function minifyCode() {
      const code = document.getElementById('inputCode').value;
      const minified = code
        .replace(/\s{2,}/g, ' ')            // Replace multiple spaces with single space
        .replace(/\n/g, '')                 // Remove new lines
        .replace(/\t/g, '')                 // Remove tabs
        .replace(/<!--.*?-->/g, '')          // Remove HTML comments
        .replace(/\/\*.*?\*\//gs, '');    // Remove CSS/JS block comments
      document.getElementById('outputCode').value = minified.trim();
    }
  </script>
</body>
</html>
